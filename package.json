{
  "name": "grade-management-system",
  "version": "1.0.0",
  "description": "A comprehensive web-based grade management system",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/init-database.js && node backend/server.js",
    "dev": "cd backend && npm run dev",
    "init-db": "cd backend && node init-database.js"
  },
  "keywords": [
    "grade",
    "management",
    "education"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
